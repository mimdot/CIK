# OpenCode Prompt — FastAPI Layer

Design REST API.

Endpoints:
- POST /profile
- GET /matches
- GET /supervisors
- GET /opportunities
- POST /feedback
- POST /bookmark
- POST /email_preferences

Use REST best practices.

Return an OpenAPI spec.

No implementation.
